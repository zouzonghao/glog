# Glog 项目架构说明

本文档旨在详细解构 Glog 前后端分离项目的架构，明确每个页面和功能点的请求流程与渲染模式。

- **前端 (Astro)**: 运行于 `http://localhost:4321`
- **后端 (Go)**: 运行于 `http://localhost:37371`

## 渲染模式定义

- **服务端渲染 (SSR)**: 页面完全在 Astro 服务器上生成。Astro 服务器会调用 Go 后端 API 获取数据，将数据与 `.astro` 模板结合生成完整的 HTML，然后发送给浏览器。**优点**: 极佳的 SEO 和首屏加载性能。
- **客户端渲染 (CSR) / API 调用**: 页面加载完成后，由浏览器中运行的 JavaScript (`main.ts`) 发起对 Go 后端 API 的直接请求 (通常是 `fetch`)。JS 获取 JSON 数据后，局部更新页面 DOM，无需刷新整个页面。**优点**: 流畅的交互体验，类似单页应用 (SPA)。
- **混合模式**: 页面的首次加载是 SSR，而页面内部的后续交互是 CSR。这是本项目交互页面的主要模式。

---

## 1. 主页与搜索页

### 1.1. 页面加载 (文章列表)

- **访问路径**: `/`, `/?page=2`, `/search?q=keyword`
- **触发方式**: 用户直接访问、点击分页链接、提交搜索表单。
- **请求流程**:
  1.  **浏览器** -> 请求 Astro 服务器 (e.g., `/?page=2`)。
  2.  **Astro 服务器** -> 调用 Go 后端 API (e.g., `/api/posts?page=2`) 获取文章列表 JSON。
  3.  **Astro 服务器** -> 将 JSON 数据渲染进 `index.astro` 或 `search.astro` 模板，生成完整 HTML。
  4.  **Astro 服务器** -> 将 HTML 返回给浏览器。
- **渲染模式**: **服务端渲染 (SSR)**
- **备注**: 这是 Astro 对内容型页面的标准处理方式，保证了性能和 SEO。

### 1.2. 列表/卡片视图切换

- **访问路径**: `/?view=cards`, `/?view=list`
- **触发方式**: 用户点击“切换视图”按钮。
- **请求流程**: 与 `1.1` 完全相同。这本质上是带了一个查询参数的链接跳转，会触发一次完整的 SSR 页面刷新。
- **渲染模式**: **服务端渲染 (SSR)**
- **优化点**: Astro 的 View Transitions 会在这里发挥作用，通过在页面刷新前后捕捉 DOM 状态，模拟出平滑的过渡动画，提升体验。

---

## 2. 新建/编辑文章页

### 2.1. 页面加载

- **访问路径**: `/admin/editor`, `/admin/editor/[id]`
- **触发方式**: 用户点击“新建”链接，或在管理页点击某篇文章的“编辑”链接。
- **请求流程**:
  1.  **浏览器** -> 请求 Astro 服务器。
  2.  **Astro 服务器** -> (如果是编辑) 调用 Go 后端 API (e.g., `/api/admin/posts/[id]`) 获取文章数据。
  3.  **Astro 服务器** -> 渲染 `editor.astro` 模板。
  4.  **Astro 服务器** -> 将 HTML 返回给浏览器。
- **渲染模式**: **服务端渲染 (SSR)**

### 2.2. 保存文章

- **访问路径**: `/api/posts` (新建) 或 `/api/posts/[id]` (更新)
- **触发方式**: 用户在编辑器页面点击“保存”按钮。
- **请求流程**:
  1.  **浏览器 (JS)** -> 拦截点击事件。
  2.  **浏览器 (JS)** -> 收集表单数据（标题、内容、发布时间等）。
  3.  **浏览器 (JS)** -> 直接向 Go 后端 API 发起 `POST` (新建) 或 `PUT` (更新) 请求，请求体为 JSON。
  4.  **Go 后端** -> 处理请求，保存文章到数据库，返回结果 JSON。
  5.  **浏览器 (JS)** -> 根据返回结果，显示通知，并可能使用 `Astro.navigate` 跳转到新的文章页或管理页。
- **渲染模式**: **客户端 API 调用 (CSR)**

---

## 3. 管理页

### 3.1. 页面加载 (文章列表)

- **访问路径**: `/admin`, `/admin?page=2`
- **触发方式**: 用户访问管理后台、点击分页。
- **请求流程**: 与 `1.1` 相同，是标准的服务端渲染流程。
- **渲染模式**: **服务端渲染 (SSR)**

### 3.2. 搜索/筛选

- **访问路径**: `/admin?q=keyword&status=private`
- **触发方式**: 用户在管理页的搜索框输入并提交。
- **请求流程**: 这将触发一次带查询参数的**页面刷新**，流程与 `3.1` 相同。
- **渲染模式**: **服务端渲染 (SSR)**

### 3.3. 批量操作 (删除、设为私密/公开)

- **访问路径**: `/api/posts/batch-update`
- **触发方式**: 用户勾选多篇文章后，点击相应的批量操作按钮。
- **请求流程**:
  1.  **浏览器 (JS)** -> 拦截点击事件。
  2.  **浏览器 (JS)** -> 收集所有被勾选文章的 ID 和操作类型 (`delete`, `set_private` 等)。
  3.  **浏览器 (JS)** -> 直接向 Go 后端 API 发起 `POST` 请求，请求体为包含 ID 列表和操作类型的 JSON。
  4.  **Go 后端** -> 处理请求，批量更新数据库，返回结果 JSON。
  5.  **浏览器 (JS)** -> 显示操作成功通知，并调用 `Astro.navigate(window.location.href)` 以触发一次由 View Transitions 控制的平滑刷新，从而展示最新列表。
- **渲染模式**: **客户端 API 调用 (CSR)**

### 3.4. 单项操作 (编辑/删除)

- **编辑**: 这是一个指向 `/admin/editor/[id]` 的普通链接，触发 SSR，见 `2.1`。
- **删除**:
    - **访问路径**: `/api/posts/[id]`
    - **触发方式**: 用户点击某篇文章的“删除”按钮。
    - **请求流程**: 与 `3.3` 类似，JS 拦截点击，直接向 Go 后端 API 发起 `DELETE` 请求，成功后调用 `Astro.navigate(window.location.href)` 实现平滑刷新。
    - **渲染模式**: **客户端 API 调用 (CSR)**

---

## 4. 设置页

### 4.1. 页面加载

- **访问路径**: `/admin/setting`
- **触发方式**: 用户点击“设置”链接。
- **请求流程**:
  1.  **浏览器** -> 请求 Astro 服务器。
  2.  **Astro 服务器** -> 调用 Go 后端 API (`/api/settings`) 获取所有设置项的 JSON。
  3.  **Astro 服务器** -> 将 JSON 数据渲染进 `setting.astro` 模板。
  4.  **Astro 服务器** -> 将 HTML 返回给浏览器。
- **渲染模式**: **服务端渲染 (SSR)**

### 4.2. 所有弹窗内的交互 (测试、保存、立即备份)

- **访问路径**:
  - `/api/settings/test-ai`
  - `/api/settings/test-imageapi`
  - `/api/settings/test-github`
  - `/api/settings/test-webdav`
  - `/api/settings/` (用于保存)
  - `/api/backup/github-now`
  - `/api/backup/webdav-now`
- **触发方式**: 用户在弹窗内点击“测试连接”、“保存设置”、“立即备份”等按钮。
- **请求流程**:
  1.  **浏览器 (JS)** -> 拦截所有按钮的点击事件。
  2.  **浏览器 (JS)** -> 收集相应表单的数据。
  3.  **浏览器 (JS)** -> 直接向 Go 后端对应的 API 端点发起 `POST` 请求。**注意**: 根据后端处理器 (`c.PostForm` vs `c.ShouldBindJSON`)，请求的 `Content-Type` 可能是 `application/x-www-form-urlencoded` 或 `application/json`。
  4.  **Go 后端** -> 处理请求，返回结果 JSON。
  5.  **浏览器 (JS)** -> 根据返回的 JSON 显示成功/失败的通知。如果操作是“保存”，则在成功后关闭弹窗。
- **渲染模式**: **客户端 API 调用 (CSR)**

### 4.3. AI 日志

- **加载日志**:
    - **访问路径**: `/api/ai-logs/`
    - **触发方式**: 用户点击“AI 日志”按钮，弹窗被打开时。
    - **请求流程**: JS 在打开弹窗的同时，向 Go 后端 API 发起 `GET` 请求，获取日志内容，然后填充到弹窗的 `<pre>` 标签中。
    - **渲染模式**: **客户端 API 调用 (CSR)**
- **清除日志**:
    - **访问路径**: `/api/ai-logs/clear`
    - **触发方式**: 用户在 AI 日志弹窗中点击“清除日志”按钮。
    - **请求流程**: JS 向 Go 后端 API 发起 `POST` 请求，成功后更新弹窗内容为“日志已清除”。
    - **渲染模式**: **客户端 API 调用 (CSR)**

### 4.4. 备份与恢复

- **下载备份**:
    - **访问路径**: `/api/backup/` (旧 SSR 路由是 `/admin/setting/backup`)
    - **触发方式**: 用户点击“下载备份”按钮。
    - **请求流程**: 这是一个**标准的链接跳转**。浏览器直接向 Go 后端请求该地址，后端生成加密的 zip 文件并通过 `Content-Disposition` 头触发浏览器下载。
    - **渲染模式**: **后端直接响应 (非 SSR/CSR)**
- **上传恢复**:
    - **访问路径**: `/api/backup/upload`
    - **触发方式**: 用户点击“上传恢复”按钮，选择文件后。
    - **请求流程**:
      1.  **浏览器 (JS)** -> 拦截文件选择事件。
      2.  **浏览器 (JS)** -> 如果是 `.zip` 文件，会弹出密码输入框。
      3.  **浏览器 (JS)** -> 使用 `FormData` (zip) 或 `JSON.stringify` (json) 构造请求体，直接向 Go 后端 API 发起 `POST` 请求。
      4.  **Go 后端** -> 处理文件，恢复数据，返回结果 JSON。
      5.  **浏览器 (JS)** -> 显示成功/失败的通知。
    - **渲染模式**: **客户端 API 调用 (CSR)**
